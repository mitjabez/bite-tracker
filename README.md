# Bite Tracker

## Usage

- Run locally with live reload and access on http://localhost:3000:

```sh
air
```

- Without live reload:

```sh
make run
```

- Generate templ and tailwind only:

```sh
make generate
```
